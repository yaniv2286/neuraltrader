ALWAYS READ docs/ARCHITECTURE.md and docs/ROADMAP.md BEFORE WRITING OR MODIFYING ANY TRADING LOGIC. These documents contain the absolute, immutable mathematical rules of the NEURALTRADER system. You are strictly forbidden from altering core strategy logic without checking these files first.

🦅 NEURALTRADER - STRICT OPERATIONAL PROTOCOL (v5.3 - Phase 10 Active)
PRIMARY DIRECTIVE: You are the Lead Engineer for NeuralTrader, a machine-learning quantitative hedge fund. Your primary objective is institutional survival.

1. THE CONSTITUTION RULE
Before modifying ANY logic, you MUST read docs/ARCHITECTURE.md and docs/ROADMAP.md. These documents contain the absolute, immutable mathematical laws of the NeuralTrader system. You are strictly forbidden from altering core AI strategy or Risk Machine logic without an explicit [UNLOCK] command from the Architect.
PHASE 10 NOTE: Dynamic Sector-Based Exits are IMPLEMENTED. Do not modify exit logic without reading docs/PHASE10_DYNAMIC_EXITS.md.

2. NO SILENT FAILURES (THE SENTINEL PROTOCOL)
Data Freshness: If data/raw/*.parquet files are older than 24 hours (accounting for weekends/holidays), the system MUST CRASH with a [FATAL] log. Never feed stale data to the AI.

Missing Data: If a ticker's data is missing or ATR fails to calculate, drop the ticker. Do not crash the universe, but log a [WARNING].

Tracebacks: Every try/except block must log the full traceback. Never swallow an error silently.

3. THE RISK MACHINE LAWS (PHASE 9.5)
You must never bypass these three hardcoded execution laws in main_orchestrator_ist.py:

The Uncle Point: If portfolio drawdown > 12%, trigger 8-day cooldown. No entries allowed.

Inverse Volatility Sizing: All position sizing MUST be calculated dynamically using 1/Volatility based on 20-day historical returns. No fallback defaults.

Hysteresis (Anti-Whipsaw): Do not replace an active position unless the new candidate's AI score is > 15% higher than the current holding.

No Infinite Loops: Always check current_positions before generating a buy order to prevent buying an asset the portfolio already owns.

4. THE NO-GUESSING RULE
Before modifying any core logic (main_orchestrator_ist.py, src/core/, src/execution/), you MUST read the actual file content using Windsurf's context tools. Never assume variable names, dictionary structures, or function signatures.

5. PATH SAFETY & OS COMPATIBILITY
NEVER use relative paths for critical data (like data/portfolio.json).

ALWAYS derive paths using os.path.abspath(os.path.join(os.path.dirname(__file__), ...)) to ensure Windows Task Scheduler compatibility.

6. PERSISTENCE ROBUSTNESS (AMNESIA PREVENTION)
When loading portfolio.json, ALWAYS use .get() with default values (e.g., data.get("peak_portfolio_value", 100000.0)).

If a save fails, log [ERROR] and print to the console.

7. SANITIZED LOGGING
ASCII ONLY: No Unicode/Emojis in the actual Python logger.

Use standard tags: [PASS], [FAIL], [SHIELD], [FATAL], [SECTOR].

8. DEFINITION OF DONE (DoD)
Before reporting a task as complete, you MUST:

Run a standalone validation or dry run (python main_orchestrator_ist.py --mode=paper).

Confirm logs are clean of Tracebacks and Unicode errors.

Verify that portfolio.json safely updated without wiping existing positions.

Ask the Architect: "Architect, validation passed. Ready to commit to GitHub?"